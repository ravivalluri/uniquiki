import { RoutesNotes, Slice, Structure } from '../interfaces';
/**
 * Creates a feature route
 */
export declare function createFeature<R, C = {}>(parentRoute: Structure, routes: RoutesNotes<R>): Slice<R & C>;
