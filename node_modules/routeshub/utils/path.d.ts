/**
 * Prevents to record empty state paths
 */
export declare function setNotEmptyPath(state: string[], path: string): string[];
/**
 * Checks multi path in a single route
 */
export declare function checkMultiPath(path: string): boolean;
/**
 * Prevents to record multi path in the state
 */
export declare function splitPath(path: string): string[];
