import { Params } from '../interfaces';
/**
 * Assigns a value based on the parent's  state and a current path
 */
export declare const setState: (parentSlice: any, path: any) => any[];
/**
 * Supports dynamic paths
 */
export declare function stateFn(state: any, params?: Params, ...otherParams: Params[]): string[];
